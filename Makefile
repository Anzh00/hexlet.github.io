install:
	pnpm install --frozen-lockfile

start:
	pnpm start

build:
	pnpm build

typecheck:
	pnpm typecheck

update-deps:
	pnpm run update-deps

# clean:
# 	bundle exec jekyll clean
#
# clean-build: clean build
