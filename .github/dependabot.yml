---
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels: [dependencies, automated, actions]
    groups:
      actions-minor-patch:
        update-types: [minor, patch]
      actions-major:
        update-types: [major]
  # Dependabot uses the "npm" ecosystem for both npm and pnpm JavaScript projects.
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    allow:
      - dependency-type: development
    labels: [dependencies, automated, dev]
    groups:
      js-dev-minor-patch:
        update-types: [minor, patch]
      js-dev-major:
        update-types: [major]
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
    allow:
      - dependency-type: production
    labels: [dependencies, automated, prod]
    groups:
      js-prod-minor-patch:
        update-types: [minor, patch]
